
export interface Module {
  icon: React.ReactNode;
  title: string;
  description: string;
}

export interface Testimonial {
  image: string;
  name: string;
  role: string;
  text: string;
}

export interface FaqItem {
  question: string;
  answer: string;
}
